# API Configuration
# 如果后端也部署在 Railway，使用 Railway 提供的 URL（不需要端口号）
NEXT_PUBLIC_API_BASE_URL=https://your-backend-name.up.railway.app

# Railway will automatically set these
# PORT=3000
# NODE_ENV=production